import styled from 'styled-components';
import { colors } from '../../utils/colors';

export const ConfigGroupTitle = styled.div`
    white-space: nowrap;
    font-weight: bold;
    font-size: 1.1rem;
    margin-bottom: 2px;
    border-bottom: solid 1px ${colors.lightGrey};
`;
